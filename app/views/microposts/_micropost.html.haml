-#Partial Displays a micropost. it is rendered in the users show page
%li
  %span.content= wrap(micropost.content)
  %span.timestamp
    Posted #{time_ago_in_words(micropost.created_at)} ago.
    = render :partial => 'shared/delete_links', :locals => { :feed => micropost }