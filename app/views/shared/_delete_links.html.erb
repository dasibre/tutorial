 <% if current_user?(feed.user) %>
    <%= link_to "delete", feed, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: feed.content %>
  <% end %>